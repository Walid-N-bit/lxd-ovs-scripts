from containers import *
from utils import *
from bridges import *


def args_func():

    pass


def main():
    # args_func()

    # vm_data = get_vms_data()
    # save_json_file(vm_data)

    # for vm in vm_list:
    #   create_bridges_for_vm(vm)
    #   br_data = get_br_data(vm)
    #   save_json_file(br_data)

    # for br in br_list:
    #   create_conts_for_br(br)
    #   conts_data = get_conts_data(br)
    #   save_json_file(conts_data)

    # for br in br_list:
    #   create_vxlans_for_br(br)
    #   vxlans_data = get_vxlans_data(br)
    #   save_json_file(vxlans_data)

    # for cont in cont_list:
    #   create_qos(port)
    #   create_queues(qos)
    #   save_json_file(data)

    # use combination() on all brs, create intent for each combination tuple

    pass


if __name__ == "__main__":
    main()
